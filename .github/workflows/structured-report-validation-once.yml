name: Structured report validation once

on:
  push:
    branches:
      - feature/jarvis-orchestration
    paths:
      - .structured-report-validation-trigger

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/jarvis-orchestration
      - run: python3 scripts/apply_structured_report_validation.py
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Core/JarvisTaskRegistry.cs Core/JarvisKnowledgeCompanion.cs Core/JarvisPolicyRegistry.cs Core/JarvisControlledTaskExecutor.cs Core/JarvisControlledExportTaskExecutor.cs Core/JarvisDocumentScopeValidator.cs Core/JarvisExecutionShadowHarness.cs
          git commit -m "Enforce structured report and export semantics"
          git push origin HEAD:feature/jarvis-orchestration
